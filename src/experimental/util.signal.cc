// util::Signal<T> is now a header-only generic template (see util.signal.hpp).
// This translation unit is intentionally empty; it is kept so existing build
// targets that list util.signal.cc continue to compile without edits.
#include "experimental/util.signal.hpp"
